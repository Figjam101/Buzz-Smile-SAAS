[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd buzz-smile-saas/server && npm install"]

[phases.build]
cmds = ["cd buzz-smile-saas/server && npm run build || echo 'No build script found'"]

[start]
cmd = "cd buzz-smile-saas/server && npm start"