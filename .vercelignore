# Exclude backups, large binaries, and unrelated projects to keep deploy size small
OpenCut/
# Allow deployment/public_html to be deployed
!deployment/public_html/**
backup_*/
project_backup_*.tar.gz
system_backup_*.tar.gz
**/*.tgz

# Only deploy the nested client; exclude server and heavy assets
server/
server/**
mongodb-portable/
ffmpeg-bin/
# Exclude large toolchains and databases
mongodb-data/
mongodb-*/

# Exclude intermediate deployment outputs
deployment/public_html/
deployment/public_html/**

# Exclude archives
*.zip
*.tar
# Prevent uploading local uploads and credentials to Vercel builds
deployment/public_html/api/uploads/
deployment/public_html/api/google-drive-credentials.json